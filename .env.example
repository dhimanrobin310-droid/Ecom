# MongoDB Atlas connection string used by the Vercel API function.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/Multikart?retryWrites=true&w=majority

# Use a long, randomly generated value in Vercel.
JWT_SECRET=replace-with-a-long-random-secret

# Create a Vercel Blob store and copy its read/write token here.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxx
